language: php

php:
  - 7.0
  - 7.1

services:
  - mysql

before_script:
  - cp .env.travis .env
  - mysql -e 'create database eloquentFilter_test;'
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit

notifications:
email:
  recipients:
    - mehdifathi.developer@gmail.com
  on_success: always
  on_failure: always
